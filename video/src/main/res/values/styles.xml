<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/texts</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="test" parent="AppTheme">
        <item name="android:actionBarStyle">@style/Holo.ActionBar.Video</item>
    </style>

    <style name="Holo.ActionBar.Video" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:displayOptions">showTitle</item>
        <item name="android:background">#b659d5</item>
        <item name="android:backgroundStacked">@null</item>
    </style>
</resources>
